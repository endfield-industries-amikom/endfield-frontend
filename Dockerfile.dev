FROM node:20-alpine
WORKDIR /app

# Install ALL dependencies (including dev — @react-router/dev, vite, tailwind, etc.)
COPY package*.json ./
RUN npm install

# Source is mounted at runtime via docker-compose volume; this COPY is a fallback
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
